version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
  build:
    commands:
      - mvn clean install
      # - apt-get install docker -y 
      # - docker build -t my-java-app:latest .
artifacts:
  files: '**/*'
  name: my-java-app
